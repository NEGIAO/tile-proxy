[project]
name = "tile-proxy"
version = "1.0.0"
description = "Standalone map tile rectify + passthrough proxy (FastAPI)"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.100.0",
    "uvicorn[standard]>=0.23.0",
    "httpx>=0.24.0",
    "pillow>=11.3.0",
]
